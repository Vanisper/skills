interface:
  display_name: "PlantUML 图"
  short_description: "PlantUML 图：编码渲染 SVG/PNG/ASCII，多后端可选"
  default_prompt: "使用 $plantuml 在 Mermaid 不够时画复杂图：写 .puml 源码，用 scripts/plantuml.mjs 编码成 server/Kroki URL 并取回 SVG/PNG 或 ASCII 文本图。"

policy:
  allow_implicit_invocation: true
